version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g pnpm
        - export WALTER_BACKEND_API_URL=https://dev-api.walterai.dev
        - pnpm install
    build:
      commands:
        - pnpm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
